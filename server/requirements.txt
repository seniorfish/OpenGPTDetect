fastapi>=0.110
uvicorn>=0.29
pydantic>=1.10
numpy>=1.24
llama-cpp-python>=0.2.70
python-dotenv>=1.0
# Optional: enable the fast PyTorch NLL backend (torch/cuda, torch/xpu or torch/cpu).
# Install it yourself; the service falls back to numpy when torch is missing.